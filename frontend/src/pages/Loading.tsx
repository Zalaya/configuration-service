import Spinner from "@/components/Panel/Loading/Spinner";

const Loading = () => {
  return (
    <div className="h-full w-full flex justify-center items-center">
      <Spinner />
    </div>
  );
};

export default Loading;
